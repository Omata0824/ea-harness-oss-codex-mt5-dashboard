# Setup Guide

1. On Windows, double-click `start-ea-harness.bat`.
2. If the browser does not open automatically, open `http://127.0.0.1:3000`.
3. Use `Setup Wizard` to confirm MT5 and Codex settings.
4. If you prefer the terminal, run `npm install` and then `npm start`.
